/* ===================================================================
   config.js — Configuracion de la API
   =================================================================== */

// IMPORTANTE: cuando el backend este desplegado en Render (o cualquier
// otro servicio), reemplazar la URL de abajo por la URL real que asigne
// el servicio, por ejemplo:
//   const API_BASE_URL = "https://proyecto-final-finanzas.onrender.com";
// Mientras tanto, esta URL local sirve para seguir trabajando en la
// propia computadora con el servidor Flask corriendo en localhost.

const API_BASE_URL = "http://127.0.0.1:5000";